[project]
name = "leaderboard-item-response"
version = "1.0.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "datasets",
    "fsspec",
    "huggingface_hub>=1.1.7",
    "numpy",
    "pandas",
    "requests",
]
